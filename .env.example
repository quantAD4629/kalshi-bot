# Example environment configuration — copy to .env and fill in real values.
# The .env file and your private key are ignored by .gitignore and must NOT be committed.

KALSHI_KEY_ID=your_kalshi_key_id
KALSHI_PRIVATE_KEY_PATH=kalshi_key.pem
